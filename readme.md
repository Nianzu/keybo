## AI policy
Any AI use MUST be attributed. Attribution must be at the level of use, and
exist in the source code. This means a comment at the top of a file or function,
a commit message is insufficient. Attribution is not required if the AI is used 
for ideation or research where code is not copied verbatim.